<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.ftc.dao.commont.FtcCommentDao">
    
	<sql id="ftcCommentColumns">
		a.id AS "id",
		a.product_id AS "product",
		a.user_id AS "user",
		a.user_name AS "userName",
		a.pic_img AS "picImg",
		a.order_id AS "order",
		a.star AS "star",
		a.content AS "content",
		a.good_count AS "goodCount",
		a.status AS "status",
		a.type AS "type",
		a.create_date AS "createDate",
		a.create_by AS "createBy.id",
		a.update_date AS "updateDate",
		a.update_by AS "updateBy.id"
	</sql>
	
	<sql id="ftcCommentJoins">
		LEFT JOIN sys_user u3 ON u3.id = a.user_id
	</sql>
    
	<select id="get" resultType="FtcComment">
		SELECT 
			<include refid="ftcCommentColumns"/>
		FROM os_comment a
		<include refid="ftcCommentJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="FtcComment">
		SELECT 
			<include refid="ftcCommentColumns"/>
		FROM os_comment a
		<include refid="ftcCommentJoins"/>
		<where>
			
			<if test="content != null and content != ''">
				AND a.content = #{content}
			</if>
			<if test="status != null and status != ''">
				AND a.status = #{status}
			</if>
			<if test="type != null and type != ''">
				AND a.type = #{type}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="FtcComment">
		SELECT 
			<include refid="ftcCommentColumns"/>
		FROM os_comment a
		<include refid="ftcCommentJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO os_comment(
			id,
			product_id,
			user_id,
			user_name,
			pic_img,
			order_id,
			star,
			content,
			good_count,
			status,
			type,
			create_date,
			create_by,
			update_date,
			update_by
		) VALUES (
			#{id},
			#{product},
			#{user},
			#{userName},
			#{picImg},
			#{order},
			#{star},
			#{content},
			#{goodCount},
			#{status},
			#{type},
			#{createDate},
			#{createBy.id},
			#{updateDate},
			#{updateBy.id}
		)
	</insert>
	
	<update id="update">
		UPDATE os_comment SET 	
			product_id = #{product},
			user_id = #{user},
			user_name = #{userName},
			pic_img = #{picImg},
			order_id = #{order},
			star = #{star},
			content = #{content},
			good_count = #{goodCount},
			status = #{status},
			type = #{type},
			update_date = #{updateDate},
			update_by = #{updateBy.id}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM os_comment
		WHERE id = #{id}
	</update>
	
</mapper>